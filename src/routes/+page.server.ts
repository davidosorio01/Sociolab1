import { db } from "$lib/server/database/client";
import { estudiantes } from "$lib/server/database/schema";


//export const load = await ()=> {
    //const temporal = await db.select().from(estudiantes)

    //console.log(temporal)
//}